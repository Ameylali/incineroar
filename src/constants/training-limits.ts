/**
 * Constants for training analysis limits and constraints
 */

/**
 * Maximum number of battles that can be analyzed in bulk operations
 * This limit is imposed to prevent performance issues and memory constraints
 */
export const MAX_BULK_ANALYSIS_BATTLES = 10000;
